export default defineAppConfig({
  nuxtIcon: {
    size: '1rem', // default <Icon> size applied
    class: 'iconify', // default <Icon> class applied
    aliases: {
      nuxt: 'logos:nuxt-icon',
    },
  },
})
